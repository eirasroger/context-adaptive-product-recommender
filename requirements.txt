# Serving dependencies, installed by every deployment.
onnxruntime>=1.30
numpy>=2

# app.frontend() arrived in FastAPI 0.138.
fastapi>=0.138
uvicorn>=0.29
pydantic>=2.6

# The served model carries its registry as YAML.
PyYAML>=6.0

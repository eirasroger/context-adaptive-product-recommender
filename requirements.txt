# Serving. This is the file a deployment installs.
#
# The model runs through ONNX Runtime. Torch trains and exports, so it lives in
# requirements-dev.txt.
onnxruntime>=1.30
numpy>=2

# app.frontend(), which serves the page, arrived in FastAPI 0.138.
fastapi>=0.138
uvicorn>=0.29
pydantic>=2.6

# The registry travels inside serve/release/model.onnx as YAML.
PyYAML>=6.0

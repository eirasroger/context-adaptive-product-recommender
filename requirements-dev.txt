# Everything needed to build data, train and test. Includes the serving set.
-r requirements.txt

pandas>=2.2
pyarrow>=15
SQLAlchemy>=2.0
alembic>=1.13

# Offline analysis for the paper. Roughly 330 MB, deliberately kept out of the
# deployment; see experiments/shap_slices.py.
shap>=0.46
numba>=0.60
llvmlite>=0.43
scikit-learn>=1.4
scipy>=1.11

pytest>=8.0
pyflakes>=3.0
